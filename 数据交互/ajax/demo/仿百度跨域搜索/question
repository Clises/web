  需求分析:
    键盘输入文本框出现联想文本；
    即：什么时候 什么条件 出现什么东西
    点击联想文本链接目标网址

   1.键盘在文本框中输入字符:
       键盘输入问题：监听鼠标松开之后产生的事件即onkeyup事件。
                     必须是字符，百度有记录，才会有关键字联想返回。
    2.如何请求到数据：
        jsonp跨域：
            数据大部分来本地服务器，无论数据的复杂程度大小，只能获取当前服务器上的数据，
         跨域解决的就是获取不同服务器下的数据。
        数据从哪来？
            百度服务器
        如何截取百度服务器上的数据？
            键盘键入之后截取network下js目录是一堆json字符串
        如何解析该字符串？
            拿出json字符串中的函数
            用script引入json字符串数据
                函数未定义问题
                    解决方式：拿出字符串中的函数,解析js字符串放到script标签下
        如何动态获取数据？
            创建script节点，键入src地址修改wd cd后内容
            将数据内容添加到ul标签之下





    网址组成问题：
        网址-路由-参数(过滤信息)
        所需要关注的两个参数: wd(关键字参数)  修改wd参数获取不同的数据
                                cb(回调函数的方法名)
         cb之后如果改的话如果没有该数据会报错404
    问题:1.如何动态修改wd参数来获取不同的关键字数据
         2.js操作元素相关
         3.什么是el？
         4.wd cb之后参数问题？








